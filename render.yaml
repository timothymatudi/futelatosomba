services:
  - type: web
    name: futelatosomba-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: bash start.sh
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      # Secrets are set in the hosting dashboard, never committed to the repo.
      - key: MONGO_DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: STRIPE_PUBLISHABLE_KEY
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        sync: false
      - key: SMTP_HOST
        sync: false
      - key: SMTP_PORT
        value: 587
      - key: SMTP_SECURE
        value: false
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASSWORD
        sync: false
      - key: EMAIL_FROM
        value: noreply@futelatosomba.com
      - key: FRONTEND_URL
        value: https://futelatosomba-frontend.vercel.app
      - key: CLIENT_URL
        value: https://futelatosomba-frontend.vercel.app
    healthCheckPath: /api/health
    autoDeploy: true
